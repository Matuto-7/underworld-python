# 🧠 Aula 09 – Manipulando Dados com input()
# O Guanabara mostra como tratar os dados que o usuário digita com input().
# Por padrão, tudo que vem do input() é do tipo string (texto), mesmo que pareça número.
#
# Por isso, ele ensina como converter esse valor para outros tipos:

# - int() → converte para número inteiro
# - float() → converte para número com casas decimais
# - bool() → converte para verdadeiro/falso
# - str() → converte (ou mantém) como texto

# 🔎 Exemplo:
# idade = input('Quantos anos você tem? ')         → é string
# idade = int(input('Quantos anos você tem? '))    → agora é inteiro

# Ele também mostra que podemos usar várias conversões dentro de print(), input() e cálculos direto.

# Dica da aula:
# “Se você quiser fazer conta, tem que garantir que o dado é número, não texto.”
# Prática com input() e tipos

# Exemplo 1 – Soma com conversão
n1 = int(input('Digite um número: '))
n2 = int(input('Digite outro número: '))
soma = n1 + n2
print(f'A soma entre {n1} e {n2} é {soma}')

# Exemplo 2 – Float (número com vírgula)
nota1 = float(input('Digite a primeira nota: '))
nota2 = float(input('Digite a segunda nota: '))
media = (nota1 + nota2) / 2
print(f'Sua média foi {media:.1f}')

# Exemplo 3 – Teste de tipos com type()
valor = input('Digite qualquer coisa: ')
print(f'O tipo primitivo desse valor é {type(valor)}')
print(f'É número? {valor.isnumeric()}')
print(f'É letra? {valor.isalpha()}')
print(f'É alfanumérico? {valor.isalnum()}')
print(f'Só tem espaço? {valor.isspace()}')
print(f'Está em maiúsculas? {valor.isupper()}')
print(f'Está em minúsculas? {valor.islower()}')

# Exemplo 4 – Booleano
resposta = bool(input('Digite algo: '))
print(f'O valor lógico é {resposta}')
# Se deixar vazio, o resultado é False. Qualquer outro valor será True.

# Exemplo 5 – Juntando tudo
nome = str(input('Qual é o seu nome? '))
idade = int(input('Quantos anos você tem? '))
peso = float(input('Qual é seu peso? '))
print(f'{nome} tem {idade} anos e pesa {peso}kg.')